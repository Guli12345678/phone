import { memo } from "react";

const Home = () => {
  return (
    <div className="container flex justify-center mx-auto mt-10 font-semibold font-sans text-3xl">
      <h2>Go to Phone page ➡️</h2>
    </div>
  );
};

export default memo(Home);
